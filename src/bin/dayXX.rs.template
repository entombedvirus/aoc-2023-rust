use anyhow::Result;
use aoc::runner;

fn main() -> Result<()> {
    runner(part_one, part_two)
}

fn part_one(_input: &str) -> Result<u32> {
    todo!()
}

fn part_two(_input: &str) -> Result<u32> {
    todo!()
}

#[cfg(test)]
mod tests {
	use super::*;

	#[test]
	fn test_part_one() -> Result<()>{
		let input = r#""#;
		assert_eq!(part_one(input)?, 0);
		Ok(())
	}
}
